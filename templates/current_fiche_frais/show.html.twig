{% extends 'base.html.twig' %}

{% block title %}FicheFrais{% endblock %}

{% block body %}
    <h1>FicheFrais</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_frai.id }}</td>
            </tr>
            <tr>
                <th>NbrJustificatif</th>
                <td>{{ fiche_frai.nbrJustificatif }}</td>
            </tr>
            <tr>
                <th>MontantValide</th>
                <td>{{ fiche_frai.montantValide }}</td>
            </tr>
            <tr>
                <th>DateDerniereModif</th>
                <td>{{ fiche_frai.dateDerniereModif ? fiche_frai.dateDerniereModif|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ fiche_frai.mois }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_current_fiche_frais_index') }}">back to list</a>

    <a href="{{ path('app_current_fiche_frais_edit', {'id': fiche_frai.id}) }}">edit</a>

    {{ include('current_fiche_frais/_delete_form.html.twig') }}
{% endblock %}
