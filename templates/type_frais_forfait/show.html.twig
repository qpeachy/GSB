{% extends 'base.html.twig' %}

{% block title %}TypeFraisForfait{% endblock %}

{% block body %}
    <h1>TypeFraisForfait</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_frais_forfait.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_frais_forfait.libelle }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ type_frais_forfait.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_frais_forfait_index') }}">back to list</a>

    <a href="{{ path('app_type_frais_forfait_edit', {'id': type_frais_forfait.id}) }}">edit</a>

    {{ include('type_frais_forfait/_delete_form.html.twig') }}
{% endblock %}
